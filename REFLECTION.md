1. Войдут в публичный интерфейс следующие функции (названия в них сменятся на подходящие для множества):
newTree()
bstFree()
bstInsert()
bstContains()
bstDelete()
bstSize()
iteratorInit() (и прочие связанные с итератором)

2. Функции обхода дерева, высчитывания высоты

3. Заменить в заголовочном файле Tree на Set (все детали реализации и так скрыты)
